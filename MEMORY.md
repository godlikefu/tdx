# MEMORY.md — 项目记忆文档

> 本文件按 AGENTS.md 第 4.5 节要求维护，记录项目演进过程中的关键结构、约定、决策与踩坑，避免协作时反复回溯。

## 项目概况

- 通达信(tdx)协议 Go 实现：标准行情(7709端口) + 盘后数据(report file) + 扩展行情(exhq)。
- 支持沪深北交易所、K线/分时/成交、板块、财务、复权、行业归属、报表配置等。
- **输出数据约定**：所有生成/落盘的数据文件统一写入 `./output/`（已入 `.gitignore`），按场景建子目录；禁止散落写入项目根目录/`example/` 内或其他任意路径（见 AGENTS.md 第 8.2 节）。

## 技术栈

- Go 1.23+，依赖 `github.com/injoyai/conv`(类型转换)、`github.com/injoyai/logs`(日志)。
- 参考开源：gotdx / mootdx / pytdx / tdx2db / nieqx-tdx。

## 架构

- `protocol/`：协议帧编解码与数据结构（`model_*.go`、`unit.go`、`frame.go`、`types.go`）。
- `client.go` / `client_exhq.go`：连接与业务 API。
- `extend/`：离线数据读取(`local.go`)、行情拉取、爬虫、HTTP server、指标计算(`model_kline.go`)。
- `lib/`：bse(北交所官网爬虫,已弃用)、gbbq、xorms、zip 工具。
- `example/`：大量独立示例。

## 关键约定与决策记录

0. **mac 协议方言已正式集成进 tdx (2026-08-30, 含 0x122B 批量行情 + 并发分页)**：外层传输信封与标准 7709 **完全相同**（16 字节头 magic `b1cb7400`/zipflag(0x0C未压/0x1C已压)/SeqID=请求[1:5]回显/Method=请求[10:12]回显/zipsize/unzipsize + zlib + 3 条 setup 握手），仅**内层命令帧**不同：mac 帧 = `[0]=0x1C + [1:5]=MsgID + [5]=0x01(version) + [6:10]=len×2 + [10:12]=命令号 + body`，与标准帧结构同构——**`[1:5]` 可携带 MsgID 且响应精确回显（已实测）**，因此 tdx 的 MsgID/Wait/缓存机制无需任何改动，直接用 `Frame{Prefix: PrefixMac, Control: Control01, Type: mac命令号}`。**服务器是独立 mac 池**（MacHosts: 121.36.248.138/123.60.47.136/121.37.207.165，与 A 股池不重叠），端口同 7709，握手需 3 条 `MacSetupCommands`（响应 Type 前两条 0x000D=TypeConnect、第三条 0x0FDB=TypeLogin2，均已加忽略 case），心跳复用第一条。**0x122F 逐笔成交**：请求体 `market(H) + code(22B补零) + ymd(I,0=最近交易日) + start(I,u32,最新端往回) + count(H) + 10B零`；响应 39B 头（count@29、total@35）+ count×18B 定长记录 `<IfIIH`（当日秒 u32/价格 f32 元→厘/量**手** u32/成交笔数 u32/Status u16：0买1卖2中性5盘后）；**页内升序、页间由新到旧**；`getMacTradeAll` 首页探测 total 后剩余页**并发拉取（限流 macMaxConcurrency=5）**再依次前插拼接（i 从 0→rest-1，方向曾写反被实连测试抓出）。底层并发安全依据：`tcp.NewDial` 返回裸 `net.Conn`（无 bufio，每帧一次 Write 系统调用）、`Wait.Entity` 键级隔离 + `maps.Safe` + 原子 MsgID——同一连接并发 `SendFrame` 安全（已实测）。**0x122B 批量自定义字段报价（实时，GetMacQuote）**：请求 = 20B 位图（前16B 字段位图 bit i=字段号、后4B 控制区0）+ count(H) + count×(market(H)+code(22B))，单次上限 **80 只**（自动分批）；响应 = 位图回显 20B + total(I) + rowCount(H) + rowCount×(68B 头：market(H)+code(22B)+name(44B GBK) + 字段数×4B 值按 bit 升序)。已请求字段 25 个（0x00 昨收/0x01-04 OHLC/0x05 量/0x06 量比/0x07 额/0x08 内盘/0x09 外盘/0x0A 0B 总流通股(万)/0x10 PE动/0x1A 现量/0x1B 换手/0x20 21 涨跌停价/0x25 涨速/0x26 均价/0x2E 盘后量/0x30 31 PE TTM 静/0x38 **今日主力净流入(元)**/0x68 量涨速/0x6C 主力净比）。**单位实锤（601872 实测自洽）**：量=手（内盘+外盘=总量、均价×股数=额、换手=量/流通股 全吻合）、价格 f32 元→厘、主力净流入为**通达信自家口径**（-1.05亿 vs 腾讯 -8818万，同数量级不同口径）。**修正 easy_tdx 类型表一处错误**：0x2E 盘后量实为 float32（实测 1192547328=float32(38100.0) 位模式）非 `<i` int32。协议还有 0x123D 竞价（**未实现**：tdx 0x056A 覆盖开盘+收盘两段是超集）。**2026-08-30 第二批扩展已实现**：① 0x1218 JSON 查询——同一命令号用帧头 head_flag(Prefix 字节=1/2)区分子命令，head=2 资金流向(Stock_ZJLX)/head=1 所属板块(Stock_GLHQ)，请求 `market(H)+code(8B)+16B零+label(21B)`，响应 27B 头+GBK JSON；资金流向 JSON=[[主入,主出,散入,散出],[5日6项]]，**与 0x122B 主力净额同源**（实测同为 -104935744）；所属板块行=[板块类型,板块市场,板块代码,板块名,现价,昨收]；两个子命令响应 Type 相同(0x1218)，**用 SendFrame 缓存参数携带 MacCapitalFlowCache/MacBelongBoardCache 按类型分发**（handlerDealMessage 的 val 缓存机制本就为此设计）。② 0x122C 板块成分报价——请求 `board_code(I)+9B零+sortType(H)+start(I)+page(H)+sortOrder(B)+0(B)+16B位图+4B控制区(0/排除位/0/1扩展)`；**板块显示代码必须转协议代码**（880216→20216, 399372→30372, 899050→32050, 000686→31686, 规则来自 opentdx；用显示代码 881160 直接请求返回空——已实测踩坑）；行格式与 0x122B 完全相同（decodeMacQuoteRows 共用），按涨幅降序实测返回上海板块真实成分。③ 0x120F 交易时段——固定 68B 请求(`04002d31`+8零+`000027060e`+52零)，响应 count(H)+8+3+9 + today(I) + ts(4) + 两组时段(各8H=4对开收分钟数) + flag + lastday(I) + ts + 2×param。④ 0x124A K线总量——请求 `<II5x`(offset=0,count)，响应 total(**大端序特例**)+returned(LE)。⑤ 0x1215/0x1217 远程文件——元信息=offset(I)+名字(70B)+30B零，响应 offset+size+flag(i8)+hash(32B ascii)；下载=index/offset/size(III)+名字100B，响应前8B保留后为内容；GetMacFile 自动分段(30KB/段)。**新增文件**：`protocol/mac.go`、`protocol/mac_quote.go`、`protocol/mac_test.go`、`protocol/mac_quote_test.go`、`hosts_mac.go`、`client_mac.go`、`client_mac_test.go`（实连测试 `TDX_MAC_LIVE=1 go test . -run TestMacTradeLive`）、`dial.go`+NewMacRangeDial、`client.go`+TypeMacTransaction/TypeMacQuote/TypeLogin2 分发、`example/GetMacTrade`。验证：单测 6 个全过；实连 20260828 sh601872 逐笔 4597 笔与 easy_tdx 逐行 100% 一致（竞价 09:25:01/19.00/7907手/626笔）。踩坑：0x122F 价格字段是 **float32 位模式**（测试数据须 `math.Float32bits` 写入）；PoC 脚本在 `output/txn_compare/poc_mac/`、`poc_msgid/`（独立 main 目录）。
1. **北交所市场编码**：`ExchangeBJ=2`（SZ=0/SH=1/BJ=2，通达信官方文档 + gotdx 交叉验证）。
2. **量化枚举齐全**：`protocol/types.go` 已按通达信量化官方 Dict(https://help.tdx.com.cn/quant/docs/markdown/Dict.html) 补全市场类型(ExchangeNQ=44新三板/SHO=8/SZO=9/HK=31/US=74等)、OrderType、PriceType、Status、DividendType、Period 枚举。Exchange 类型是 uint8，注意 `ExchangeINE` 与 `ExchangeSHF` 同为 30。
3. **标准行情协议不支持 BJ 代码列表请求**：`GetCount/GetCode(market=2)` 超时；但行情/K线正常。
4. **北交所代码获取**：`GetCodeAll(ExchangeBJ)` 走 `GetZHBFiles()` → `tdxbjmore.cfg`（含名称，338条，含已分配未上市代码）。弃用官网爬虫（335条，仅已上市）。
5. **tdxstat.cfg**：全市场逐股统计，35字段，无名称字段。名称需另从 `tdxbjmore.cfg`/板块文件等获取。
6. **退市股票**：强制退市→新三板(代码400/420，市场44)；通达信已清退原代码K线数据，线上查不到退市股K线。实测(2026-08)：标准行情7709 `GetKlineDay("600001"/"sh600001")` 返回0条、`GetQuote(sh600001)` 全零；扩展行情7727 `ExQuote/ExBars` 在 market 0/1/2/44 × (原代码/`T600001`) 全部返回全零——标准协议接口均查不到退市股数据(对照组 `sz000609` 正常, 排除网络/协议问题)。客户端能显示退市股(如 `T600001`)是通过【功能→定制品种→退市品种管理】绑定"自设市场+原代码"后，其历史日线缓存于本地 `<安装目录>/T0002/ds_cache/`，文件命名 `<市场标识>#<代码>.~~~day`(实测 `100#T600001.~~~day`/`100#T600003.~~~day`，`100#` 为缓存命名空间/市场标识，`~~~day` 为日线缓存后缀)，非实时行情服务器提供、也不在 `vipdoc/lday` 下。**缓存格式与标准 `.day` 不同**：头部14字节(`[0:10]`保留 + `[10:14]` uint32 记录数)，之后每条32字节——`[0:4]`uint32日期YYYYMMDD、`[4:20]`4×float32开/高/低/收(**元**)、`[20:24]`float32成交额(元)、`[24:28]`uint32成交量(股)、`[28:32]`保留。读取示例：`example/ReadLocalDelisted`。
7. **板块指数(880xxx/881xxx)编码约定**：`protocol.IsIndex/AddPrefix` 已识别板块指数——880xxx(概念/风格/地区)、881xxx(行业)归属上海交易所(ExchangeSH)，`AddPrefix("880741")`→`sh880741`，`IsIndex("sh880741")`=true。板块指数日K线用 `GetIndexDay`(指数解析, 量×100/涨跌家数)、实时行情用 `GetQuote` 均可直接获取；`GetQuote` 已改为对指数类(含板块指数)跳过 `DefaultCodes` 价格修正(指数原始解码价格即正确, 不再要求 DefaultCodes 初始化)。板块指数不在 `GetCodeAll`/`DefaultCodes` 股票代码列表内。示例：`example/GetBlockData` 演示拉全量板块+板块指数行情。
8. **可转债(标准行情7709)**：`protocol.AddPrefix` 已识别可转债前缀——沪市(110/111/113/118)→`sh`、深市(123/125/126/127/128)→`sz`。日K线 `GetKlineDay`/分钟线直接可用(价格解码与股票同路径, 分→厘)。实时行情 `GetQuote` 依赖 `DefaultCodes` 价格修正：转债 `Decimal=4`(价格×10^(2-4)=÷100)，故已收录在市转债价格正确；已退市/未收录转债不在 `DefaultCodes` 内会报"未查询到代码"。当前在市转债约326只(沪深GetCodeAll实时列表可查)。
9. **期货(扩展行情7727)**：`DialExHqDefault` 连通, 走 `client_exhq.go`。合约代码格式=`品种+YYMM`(如 `IF2609`、`A2609`)，`IF00` 等连续/主力代码无效。期货批量行情用 `ExQuoteList(market, 3, 0, n)`(category=3, market: 47中金/60主力期货/30上期/28郑商/29大商/66广期)，返回收/昨结/持仓/量。期货日K用 `ExBars(4, market, code, 0, n)`(扩展行情日K category=4, 与标准行情 Day=9 不同; 时间/价格/持仓/量/结算价全部正确)。`ExInstruments` 分页 start 为全局品种序号(非市场编号), 全市场约14.4万品种, 含通达信商品指数(T001~T032, market=42)。
10. **`DecodeCode` 通用代码解析**：已泛化支持多市场——A股(6位数字自动补前缀, 行为不变)、港股(5位纯数字如 `00700`/`hk00700`)、美股(纯字母如 `AAPL`/`usBRK.B`, 最长前缀匹配避免 `SHOP` 被误拆为 `sh`+`OP`)、期货(需显式前缀如 `cffIF2609`/`dceA2609`, 裸合约如 `IF2609` 因无法确定交易所而报错提示用前缀)。另支持带点后缀格式 `000001.SZ`/`600000.SH`/`00700.HK`/`AAPL.US`/`IF2609.CFF`(后缀=交易所缩写, 大小写均可; 美股点代码 `BRK.B` 因后缀 B 非交易所而按美股代码解析)。前缀支持小写缩写/大写/中文名(如 `上海600000`)。注意: 标准行情7709的 Frame(model_quote/model_kline 等)只接受 A股 6 位定长代码; 港股/美股/期货实际走扩展行情7727(`ExQuote`/`ExBars` 等), 不经 DecodeCode。
11. **分时成交(GetTrade/GetMinuteTrade)分页语义**：`start` 是**从最新一笔往回偏移**(start=0 返回最后 1800 笔, 即尾盘+盘后固定价格交易), 不是从开盘往后; 服务器单次最多返回 1800 条(`count-start<=1800`)。全量拉取用 `GetTradeAll`/`GetMinuteTradeAll`(自动按 start+=1800 往回翻页, 并 prepend 拼成正序), 只能盘后调用(盘中翻页间隔内新增成交会丢)。无 status 的尾盘记录(15:05~15:30 固定价)是沪市主板盘后固定价格成交。首条 09:25 为集合竞价汇总(中性)。`Trade.Time` 的日期部分是客户端按当日戳的(`TradeCache{Date: time.Now()}`), 实际数据属于最近一个交易日。**与 easy_tdx 三方对账(sh601872, 2026-08-28, 4597 笔)结论**：tdx 与 easy_tdx 标准协议逐行 100% 一致(价格/量/方向), 与其 mac 协议 0x122F(秒级)排序后也逐行 100% 一致; **`Trade.Number`=成交笔数已实锤**(与 mac trade_count 4597/4597 全等, easy_tdx 侧与东财对账过, 合计 157,343 笔)——注释里"不一定有值"可改为确认语义; `Status=5` 实测存在于盘后固定价成交(15:05+ 共 30 笔), 但 `StatusString()` 只映射 0/1/2(5 打印为空), 待补映射; 沪市连续竞价 tick 实为 3 秒快照聚合(单分钟≤20 笔, 秒值 01/04/.../58), easy_tdx mac 0x122F 提供真实秒级(盘后段秒值不规则可证非合成), tdx 分钟精度丢失该信息; easy_tdx 标准协议"当日"查询在非交易日返回空(按字面日期), tdx 不带日期返回最近交易时段。**秒级 tick 无法复现厂商主力/资金流向数据**(实测 sh601872 对腾讯 hsfundtab 接口, 同日 2026-08-28)：按腾讯口径(单笔≥20万或≥6万股)重算主力净流入 -765万 vs 官方 -8818万, 差 91%; 阈值扫描 10万~400万最好仍差 61%; 成交额守恒成立(秒级 16.52亿×2=腾讯买卖两侧 33.04亿)但主力买占比 86.4% vs 官方 37%。根因: 腾讯按逐笔成交回报(委托单粒度)统计, 3 秒聚合档金额膨胀(散户打包成主力)/拆细退化(子单均值仅~1万), 且 3 秒档单方向标记丢失档内双向信息; 竞价 1.5 亿中性单的归属假设引起 ±1.5 亿摆动。分钟形状相关仅 0.67。结论: 主力资金口径必须逐笔/L2 源, tick 数据只适合微观结构分析。
12. **集合竞价(CallAuction, 命令 0x056A)**：返回最近交易时段**开盘竞价(09:15~09:25) + 收盘竞价(14:57~15:00)** 两个时段的 3 秒级虚拟撮合快照(601872 实测 229 条, 其中收盘段 61 条; 与 python 项目 easy_tdx 的 mac 协议 0x123D 逐行对账一致, 但 0x123D 只返回开盘段 168 条——**tdx 的 0x056A 数据覆盖更全**)。**请求体字段已实测**(601872 变异实验)：`Data = exchange[0] + 0x00[1] + code[2:8] + A[8:12] + B[12:16] + C[16:20] + D[20:24] + E[24:28]`(uint32 LE)，其中 **D=start(写1跳过首条, Count 229→228)、E=count(写10返回前10条 09:15:01~09:15:28, 写1000返回全部229, 是上限非固定值)**——协议支持分页但 tdx 的 `Frame(code)` 写死 E=500 未暴露；当前数据量理论上限≈262条(开盘201+收盘61, 3秒一条) < 500 不会截断，但若快照加密/时段增多会**静默丢尾部(收盘段)**；A=1 会导致无数据(疑似保留/校验字段)，B=1/2、C=1 无影响(非时段掩码非start)。竞价快照时间到秒(分笔成交无秒)。`Unmatched` 4 字节有符号: 正=买盘未匹配排队, 负=卖盘(easy_tdx 文档标"符号含义待定", 其实与 tdx 语义一致); 09:25:00 快照是指示性虚拟匹配(如 19.04/6323手), 最终真实撮合以分笔 09:25:00 打印为准(19.00/7907手)。注意: easy_tdx 还从行情包 ReversedBytes2/3 反解出了"集合竞价成交金额"(open_amount), tdx 的 `Quote.ReversedBytes2/3` 仍是未知字段, 可参考 easy_tdx `models/quote.py` 的结论反解。探测工具: `output/auction_probe`。
13. **依赖升级记录 (2026-08-30, 两档全完成)**：第一档 x 系列安全升级——`golang.org/x/text v0.34.0→v0.41.0`（GO-2026-5970, 直接依赖, protocol/unit.go GBK 解码热路径）、`x/net v0.50.0→v0.58.0`（清 9 个漏洞 GO-2026-4559~5942）、`x/sys v0.42.0→v0.47.0`（GO-2026-5024）。第二档驱动升级——`go-sql-driver/mysql v1.7.0→v1.10.0`（要求 go1.24, 新增间接依赖 filippo.io/edwards25519）、`glebarez/go-sqlite v1.22.0→v1.23.0`（带入 modernc.org/sqlite v1.28→v1.55/libc v1.74 大跳 + ncruces/go-strftime），运行时冒烟 `example/WorkdayAndCodes`（NewWorkday+NewCodesSqlite 实连建库）通过。升级后 govulncheck **完全清零**（升级前 0 可触达+11 模块级）。工具链 go1.27.0 已是最新（goproxy toolchain 模块证实）；go.mod 指令保持 `go 1.25.0`（公开库下限友好，x/text v0.41.0/glebarez v1.23.0 恰好要求 1.25.0）。**未升**（有意保留）：xorm v1.3.9（可升 v1.4.1, ORM 行为需回归, 当前无已知问题）、injoyai/* 与 cron 已是最新、xorm.io/core 已停更(v0.7.3 终版)。沙箱环境注意：go get/sumdb 校验需 `GOPATH=/Users/fushixiang/tdx/.gocache/gopath`（默认 GOPATH 的 sumdb 缓存目录在沙箱外会被拒），配合 GOCACHE/GOMODCACHE/GOPROXY=goproxy.cn。**测试基线事实**：`go test ./extend/` 的 TestReadDay/TestReadMinute/TestReadDayBJ 升级前后同样失败——测试硬编码作者 Windows 路径 `D:\软件\通达信`，macOS 上属环境性预存失败（TestWrite* 与其余全过），与依赖无关。
14. **日K线(GetKlineDay)语义与标准池连接实测 (2026-08-30)**：① `GetKlineDay(code, 0, N)` 返回 List 为**时间升序(最老在前)**, start=0 锚定最近 N 根——与 `GetTrade`(start 从最新端往回翻页)语义不同, 勿惯性翻转; ② K线 `Amount` 单位=**厘(元×1000)**与 Price 同单位(验算: sh601872 2026-08-28 量 864362 手×均价 19.107 元=16.51 亿元, 与 0x122B 实时 amount 一致), Volume=手; ③ 数据交叉验证: 日K收盘 18.980=0x122B 现价、昨收 18.620=0x122B PreClose、开盘 19.00=0x122F 竞价首笔价, 全部吻合; ④ **标准 7709 池默认 `DialDefault`(Hosts 轮询)在部分网络下逐台慢重试可卡数分钟**(实测一次 8 分钟才连上), `tdx.Dial("host:7709")` 直连单台秒回——实测可用 124.71.187.122:7709; ⑤ 演示脚本: `output/kline_601872/`(60 交易日 CSV 落盘)。
15. **httpserver mac 路由 (2026-08-30)**：`extend/httpserver` 新增 `/mac/*` 共 9 条路由(quote/trade/trade/all/trade/history/capital_flow/belong_boards/board_members/server_session/kline_count), `WithMacHosts(...)+WithMacPoolSize(n)` 启用独立 mac 连接池(未配置则路由不注册); 实测延迟: 标准与 mac /quote 均 ~37-42ms(池化长连接复用), 资金流 98ms, 全量 4597 笔秒级逐笔 142ms。演示程序 `output/httpserver_demo/`。另提供 `cmd/tdx-api` 命令行启动器(flags: -addr/-pool/-mac/-mac-pool/-ex/-debug), `go run ./cmd/tdx-api` 一键起双池服务——注意模块根目录是 package tdx 库本体, 不能放 package main(同目录双包冲突), 故按 Go 惯例放 cmd/。
16. **mac 0x122B 五档盘口 (2026-08-30)**：字段位表实为 **20B=160 位**（位 128-159 与控制区重叠），服务器实测**支持 0x80+ 盘口位**（easy_tdx 的 HANDICAP 预设因 parse 只取位图前 16B 而丢失 3-5 档——其实现有缺陷）：买一价 0x11/卖一价 0x12/买量 0x18/卖量 0x19/委比 0x39/买二价 0x48/卖二价 0x49/买二量 0x5D/卖二量 0x5E/买三~五价 0x80-82/卖三~五价 0x83-85/买三~五量 0x86-88/卖三~五量 0x89-8B（<f 价 + <I 量）。GetMacQuote 已扩至 46 字段含完整五档（MacQuote.Bids/Asks = [5]MacLevel{Price,Vol} 档位数组 + EntrustRatio 标量），macQuoteFieldDef.bit 需 uint16；解码含**长度一致性回退**（若高位位被原样回显但不打包值，按数据长度自动回退低 128 位）。实连校验：买价降序/卖价升序/买一≤卖一 全过（601872 买一 18.98×364 手, 卖一 18.99×302 手）。

## 本地数据文件解析（extend/local.go，参考 pytdx TdxDailyBarReader/TdxLCMinBarReader 官方协议）

- 目录结构：`vipdoc/<sh|sz|bj>/lday|minline|fzline`
- **`.day` 日线**：32字节/条
  - `00~03` uint32 日期(YYYYMMDD)
  - `04~19` 4×uint32 开/高/低/收(价格×100 整数)
  - `20~23` float32 成交额(元)，`24~27` uint32 成交量(股)，`28~31` 保留
- **`.lc1`(1分钟)/`fzline/*.lc5`(5分钟)**：32字节/条
  - `00~01` uint16 日期(year=num/2048+2004, month=(num%2048)/100, day=num%2048%100)
  - `02~03` uint16 当日分钟数(0点起)
  - `04~19` 4×float32 开/高/低/收(元)，`20~23` float32 额(元)，`24~27` uint32 量(股)，`28~31` 保留
- 单位约定：内部 `Price` 单位=厘(元×1000)，`Volume` 单位=手(股÷100，与线上K线一致)。
- **成交量单位差异（指数 vs 股票）**：`.day/.lc1/.lc5` 的 `24~27` 字段，**指数(如 sh000001/sz399001/bj899050)单位是"手"，原值即手**；**股票单位是"股"**，需 ÷100 转手。`ReadDay/ReadMinute1/ReadMinute5/WriteDay/WriteMinute1/WriteMinute5` 已按 `protocol.IsIndex(c)` 区分处理（指数不÷100；写入时股票×100转股、指数原样写手）。判断时用 decodeCode 已带前缀的 c 直接 `IsIndex(c)`，勿再拼前缀。
- API：`ReadDay(dir, code)`、`ReadMinute1(dir, code)`、`ReadMinute5(dir, code)`；code 需带交易所前缀(如 `sz000001`)，本地文件名为 `sz000001.day` 格式。
- **写入**：`WriteDay(code, ks) ([]byte, error)`、`WriteMinute1(code, ks)`、`WriteMinute5(code, ks)`（均 `([]byte, error)`），与读取格式对称，**只返回通达信格式字节流、不落盘**，由调用方自由决定如何写入/使用（如 `example/FetchLC1ForTest` 内自行 `os.WriteFile` 到 `./output/lc1/vipdoc/...`）。code 需带交易所前缀用于判断指数/股票。均在 `extend/local.go`。

## 踩坑记录

- **`.day` 价格是 uint32×(100)** 而非 float32——旧实现用 bytesToFloat 解析价格/成交量完全错误，日期解析也有误（现已修复）。
- **分钟线价格/成交额是 float32**，必须用 `math.Float32frombits` 转，直接 `Uint32` 得位模式会得到天文数字价格。
- **float32 精度限制**：分钟线价格以 float32(元) 存，两位小数价格经 float32 往返会差 ±1厘（如 11.2 元→11.199999），测试断言需容差。
- **`.lc1` 占位记录（与 `.lc5` 的差异，重要）**：真实 `.lc1` 每个交易日末尾（14:59=899分钟，偶见 14:58=898）有 1 条「量=0额=0」的占位记录，其 OHLC 价格=当日最后成交价（四价相同）；当日首条 09:31 是**真实数据**（量额非零），绝不能重复/跳过。`.lc5` 无此占位。`ReadMinute1` 仅对 `.lc1` 跳过「分钟>=898(14:58)且量额全零」的记录（`ReadMinute5` 全保留）；`WriteMinute1` 生成 `.lc1` 时在**当日最后一条真实数据(15:00)之前**补 14:59 占位（`WriteMinute5` 不补）。注意：**不能只按「量=0额=0」判断占位**——实时拉取的数据里盘中也有量额全零的真实无成交分钟（实测 sz000001 有 101 条），必须同时满足「分钟>=898」才跳过，否则误删。用户生成 `.lc1` 若显示不了成交量，多半是占位/量零记录问题。
- **踩坑（用被覆盖文件误判格式）**：曾用 `sz000001.lc1` 分析真实格式，但它已被本项目 `WriteMinute1` 生成的旧文件覆盖，导致误判「占位在当日首条前、9:31 有重复」。真实格式必须用未被覆盖的文件（如 `sz000002.lc1`）验证。教训：**分析真实格式前先确认样本文件非本工具生成**。
- **1分钟线文件巨大**（如 sz000001.lc1 超 120 万条 / 35MB），读取时注意内存；通达信 1 分钟数据可能较旧（取决于客户端下载范围）。
- `minline/` 目录只有 `.lc1`(1分钟)；5分钟 `.lc5` 实际存放在 `fzline/` 目录。
- **分钟线拉取/生成工具**：`example/FetchLC1ForTest/main.go` 实时拉取股票/指数 1分钟与5分钟K线，用 `WriteMinute` 生成 `.lc1/.lc5`，输出统一放 `./output/lc1/vipdoc/<sh|sz>/<minline|fzline>/`（供客户端导入测试，需手动复制到通达信 `vipdoc` 目录）。指数分钟线用 `GetIndexAll(TypeKlineMinute/5Minute)`，股票用 `GetKlineMinuteAll`/`GetKline5MinuteAll`。
- zhb.zip 盘后包内含 46 个配置（tdxstat/tdxstat2/tdxbjmore/tdxhy/tdxzs/tdxbk/gbbq 等），GBK 文本，解析需 `UTF8ToGBK`。
- **AI 沙箱内运行示例/测试**：默认 Go 构建缓存 `~/Library/Caches/go-build` 与模块缓存 `~/go/pkg/mod` 在沙箱内不可写（`operation not permitted`），需重定向到 workspace：`GOCACHE=/Users/fushixiang/tdx/.gocache/go-build GOMODCACHE=/Users/fushixiang/tdx/.gocache/gomod GOTELEMETRY=off go run .`（依赖首次会重新下载）。另：`NewCodesSqlite`/`NewGbbq` 等的 sqlite 缓存默认写在**相对 CWD** 的 `./data/database/`（已 gitignore），在哪个目录运行就落在哪；README 快速开始示例 `example/GetQuote` 末尾 `select {}` 永久阻塞，验证完输出需手动停进程。
- **同级项目 easy_tdx**（`/Users/fushixiang/tdx/easy_tdx`，Python）：通达信 mac 协议方言(帧头 0x1C 10字节头, 同样连 7709 端口, 命令号不同如竞价 0x123D vs 标准 0x056A)，含 pandas/异步/CLI/web-ui。其 `.venv` 建于旧路径 `~/code/easy_tdx`，搬家后 python symlink 失效——直接用 `.uv-python/cpython-3.13.10-macos-aarch64-none/bin/python3.13` + `PYTHONPATH`(src 与 .venv/lib/python3.13/site-packages) 可运行；配置目录用 `EASY_TDX_CONFIG_DIR` 重定向(默认 `~/.easy_tdx` 沙箱内不可写)。其 `AuctionItem.unmatched` 保留带符号原值, 与 tdx `CallAuction.Flag` 语义一致。